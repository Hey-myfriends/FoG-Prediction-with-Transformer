from .FoG_Net import build as build_model

# def build_model(in_chan, d_model, num_class, cls_type="cls_token", aux_loss=True):
    # return build(in_chan, d_model, num_class, cls_type=cls_type, aux_loss=aux_loss)